/** @type {import('next').NextConfig} */
const nextConfig = {
  // Remove the experimental.appDir line completely
  // appDir is now stable and enabled by default in Next.js 14
}

module.exports = nextConfig